import assert from"node:assert/strict";import test from"node:test";import{parseControlFollowup}from"../src/control-followup.mjs";
test("focused move uses a valid control-only target",()=>{const p=parseControlFollowup("put the focused window on the left",{focused_panel:{command:"G",security:"META"}});assert.deepEqual(p.steps[0].target,{mode:"focused",command:null,security:null});});
test("resize correction keeps only the corrected direction",()=>{const p=parseControlFollowup("make this smaller no sorry larger",{focused_panel:{command:"ERN",security:"META"}});assert.equal(p.steps[0].operation,"resize");assert.equal(p.steps[0].value,"larger");assert.equal(parseControlFollowup("make this smaller and larger",{focused_panel:{command:"ERN",security:"META"}}),null);});
test("HMAP view plus unverified export never partially executes",()=>{assert.equal(parseControlFollowup("make this heatmap a table and export it",{focused_panel:{command:"HMAP",security:null}}),null);});
test("EQS verified range plus unknown add clause never partially runs",()=>{assert.equal(parseControlFollowup("set market cap above ten billion add healthcare and run it",{focused_panel:{command:"EQS",security:null}}),null);});
test("OpenAI phonetics and query corrections do not look like panel opening",()=>{const p=parseControlFollowup("search exact term open eye no wait open ai",{focused_panel:{command:"N",security:null}});assert.equal(p.steps[0].actions[0].value,"open ai");});
test("HMS normalizes percent and side-by-side ASR noise",()=>{const p=parseControlFollowup("add in video make it five years change purse sent and side by site",{focused_panel:{command:"HMS",security:"AAPL",members:["AAPL"]},resolved_entities:[{spoken_name:"in video",ticker:"NVDA",venue:"US",asset_class:"EQ"}]});assert.deepEqual(p.steps[0].actions.map(x=>x.value),["NVDA","5Y","Change percent","Side-by-side"]);});
